/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

//CODE HERE
let gitDefinition = ' Git is version control system, you have record of what has been done, you can revert versions should you need to. Helps for collaboration, allowing changes by  multiple people to all be merged into one source. Git software that runs locally'

let gitDefinitionPortu = 'O Git é o sistema de controle de versão mais usado. O Git rastreia as alterações que você faz nos arquivos, para que você tenha um registro do que foi feito e possa reverter para versões específicas se precisar. O Git também facilita a colaboração, permitindo que as alterações feitas por várias pessoas sejam mescladas em uma única fonte.Git é um software que roda localmente. Seus arquivos e seu histórico são armazenados em seu computador. Você também pode usar hosts online (como GitHub ou Bitbucket) para armazenar uma cópia dos arquivos e seu histórico de revisões. Ter um local centralizado onde você pode fazer upload de suas alterações e fazer download de alterações de outras pessoas permite que você colabore mais facilmente com outros desenvolvedores. O Git pode mesclar automaticamente as alterações, para que duas pessoas possam trabalhar em partes diferentes do mesmo arquivo e depois mesclar essas alterações sem perder o trabalho uma da outra!'

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

//CODE HERE
let gitHubDefinition = 'GitHub is a website and cloud-based service that helps developers store and manage their code '

let gitHubDefinitionPort = 'O GitHub é um site e serviço baseado em nuvem que ajuda os desenvolvedores a armazenar e gerenciar seu código, além de rastrear e controlar alterações em seu código. Para entender exatamente o que é o GitHub, você precisa conhecer dois princípios conectados. O GitHub é uma empresa com fins lucrativos que oferece um serviço de hospedagem de repositório Git baseado em nuvem.'
//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

//CODE HERE
 let gitInitDefinition = " git init command creates a new Git repository. Is the first command you'll run in a new project"

 let gitInitDefinition = 'O comando git init cria um novo repositório Git. Ele pode ser usado para converter um projeto não versionado existente em um repositório Git ou inicializar um novo repositório vazio. A maioria dos outros comandos do Git não está disponível fora de um repositório inicializado, portanto, esse geralmente é o primeiro comando que você executará em um novo projeto.'

//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

//CODE HERE
let gitCloneDefinition = 'git clone is a Git command line utility which is used to target an existing repository and create a clone, or copy of the target repository.'
let gitCloneDefinitionPort = 'git clone é um utilitário de linha de comando Git que é usado para direcionar um repositório existente e criar um clone ou cópia do repositório de destino.'

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

//CODE HERE
 let gitStatusDefinition = 'The git status command displays the state of the working directory and the staging area. It lets you see which changes have been staged, which haven t, and which files aren t being tracked by Git.'

 let gitStatusDefinitionPort ='O comando git status exibe o estado do diretório de trabalho e a área de teste. Ele permite que você veja quais alterações foram preparadas, quais não foram e quais arquivos não estão sendo rastreados pelo Git.'

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

//CODE HERE
 let gitAddDefinition =' In review, git add is the first command in a chain of operations that directs Git to "save" a snapshot of the current project state, into the commit history. When used on its own, git add will promote pending changes from the working directory to the staging area.'
 let gitAddDefinitionPort = 'Em revisão, git add é o primeiro comando em uma cadeia de operações que direciona o Git para "salvar" um snapshot do estado atual do projeto, no histórico de commits. Quando usado sozinho, o git add promoverá alterações pendentes do diretório de trabalho para a área de teste.'
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

//CODE HERE
let gitCommitDefinition ='In Git, a commit is a snapshot of your repo at a specific point in time. To help further understand what a Git commit is, we need to review your Working Directory vs your Staging Directory and how files changes are reflected in your Git repository. '

let gitCloneDefinitionPort ='No Git, um commit é um instantâneo do seu repositório em um momento específico. Para ajudar a entender melhor o que é um Git commit, precisamos revisar seu diretório de trabalho versus seu diretório de teste e como as alterações de arquivos são refletidas em seu repositório Git.'



let gitInitialCommit ='Is one Initial commit is the first commit made in a Git project. As mentioned in the previous section, developers use Git to create chains of commits that track the revision history of a project'
let gitInitialCommitPort = 'Is one Initial commit é o primeiro commit feito em um projeto Git. Como mencionado na seção anterior, os desenvolvedores usam o Git para criar cadeias de commits que rastreiam o histórico de revisões de um projeto'

//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

//CODE HERE

let gitPushDefinition = 'Push = Send changes to the master branch of your remote repository git push origin master'
let gitPushDefinitionPort =' O comando git push carrega o conteúdo do repositório local para o repositório remoto. Empurrar é o oposto de buscar. Se o git fetch importa o conteúdo para os branches locais, o git push exporta para os branches remotos.'
